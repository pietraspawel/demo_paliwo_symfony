{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" type="submit">{{ button_label|default('Zapisz') }}</button>
{{ form_end(form) }}
