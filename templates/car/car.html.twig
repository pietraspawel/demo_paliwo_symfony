{% extends 'base.html.twig' %}

{% block title %}Samochody{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('assets/js/car.js') }}" defer></script>
{% endblock %}

{% block body %}

    {{ include('car/_delete_confirmation_modal.html.twig') }}
    <div class="container">
        <div class="row">    
            <div class="col-md"> 
                {{ include('car/_add.html.twig') }}
            </div>
            <div class="col-md"> 
                {{ include('car/_index.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
